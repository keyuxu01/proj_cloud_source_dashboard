// tRPC server-side configuration (routers, context, etc.)
// Import these from the server module:
// import { appRouter, createTRPCContext } from "@/server/trpc";
// import type { AppRouter } from "@/server/trpc";

// tRPC client-side utilities
// Import these from their specific modules:
// import { TRPCProvider, useTRPC, useTRPCClient } from "@/utils/trpc";
// import { trpcClient } from "@/utils/trpc/setup";
